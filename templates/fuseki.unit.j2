[Unit]
Description=Apache Jena Fuseki
After=syslog.target network.target remote-fs.target nss-lookup.target

[Service]
Type=forking
ExecStart={{ fuseki_home }}/fuseki start
ExecReload={{ fuseki_home }}/fuseki restart
ExecStop={{ fuseki_home }}/fuseki stop

[Install]
WantedBy=multi-user.target
